/**
 * 🧬 M1.1.4 — Spacing & Layout Constants
 */
export const spacing = {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
    xxl: 48,
    screenPadding: 20,
} as const;

export const borderRadius = {
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    full: 9999,
} as const;

export const hitSlop = {
    top: 12,
    bottom: 12,
    left: 12,
    right: 12,
} as const;
